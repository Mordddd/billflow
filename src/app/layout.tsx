import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BillFlow — Invoice less. Get paid faster.",
  description:
    "Create professional invoices, send them through WhatsApp, and keep track of who has paid. Built for Indonesian freelancers and small businesses.",
  keywords: [
    "invoice generator Indonesia",
    "invoice online",
    "invoice UMKM",
    "invoice WhatsApp",
    "aplikasi invoice",
    "aplikasi penagihan",
  ],
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
